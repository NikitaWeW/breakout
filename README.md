# breakout (TODO: rename)
~~my first game.~~
~~Right now im setting up an environment for the development (ecs, renderer, scenes, parsers, implementing graphic stuff, etc.)~~
~~This is turning into a *mini(?)-*engine for some reason...~~

A relatively small game engine centered around my EC (entity component) library.

---

**TODO**: doxygen documentation :/ 

*If you really want to see how it works check out src/app first*

# building
uses cmake:

``` shell
cmake -S . -B build
cmake --build build
build/main
```

No external dependencies yet :D
